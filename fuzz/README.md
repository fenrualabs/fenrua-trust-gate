# Fuzzing Boundary

Parser and canonicalisation fuzz targets are planned for the later R3 test
train. R1 foundation and R2 prototype unit tests cover bounded parsing,
duplicate keys, Unicode surrogate handling, canonical determinism, and
canonicalisation bounds. Their presence is not a claim that fuzzing has run or
that the implementation has been audited.
