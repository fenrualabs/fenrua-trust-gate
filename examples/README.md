# Examples Boundary

No executable product examples are included at R1. A future example must be
synthetic, local-only, non-secret, explicitly tied to a released contract, and
must not present a parser/digest demonstration as an authorisation workflow.
