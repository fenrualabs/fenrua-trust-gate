# Examples Boundary

No released product example is included. The synthetic `fixtures/r2/` inputs
support the local CLI command shown in the root README, but they demonstrate an
unreleased prototype only. A future example must be synthetic, local-only,
non-secret, explicitly tied to a released contract, and must not present a
parser/digest demonstration as a production authorisation workflow.
